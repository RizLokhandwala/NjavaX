## <- this is commented text
mode 4 ## Default mode = 0. 
## Since portNo is not included in configuration, it will be left as it's default value (portNo = 8080)
landingPath /tmp/webpagefiles
## wDrive

entryList {
    serverEntry {
        hostName name
        portNumber 0001
    }
    serverEntry {
        hostName otherName
        portNumber 0002
    }
    serverEntry {
        hostName newName
        portNumber 0003
    }
}